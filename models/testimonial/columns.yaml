# ===================================
#  List Column Definitions
# ===================================

columns:
    id:
        label: mja.testimonials::lang.fields.id
        invisible: true

    is_public:
        label: mja.testimonials::lang.fields.public
        type: switch

    author:
        label: mja.testimonials::lang.fields.author

    date:
        label: mja.testimonials::lang.fields.date
        type: date
